mutation DeletePost($id:ID!){
  deletePost(id:$id){
    code,
    success,
    message
  	post{
      id
      title
      text
      userId
        points
      user{
        id
        username
        email
      }
      createdAt
    }
  }
}