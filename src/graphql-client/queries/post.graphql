query Post($id:ID!) {
  post(id: $id) {
    id
    title
    text
    createdAt
    updatedAt
    textSnippet
    userId
    points
    userId
        voteType
    user {
      id
      username
      email
    }
  }
}
